#include<stdio.h>
int main(){
  int arr[10]={1,5,6,7,9,10};
  int i,n;
  int target=6;
  printf("searching for %d");
  for(i=0;i<10;i++){
    if(arr[i]==target){
     printf("found %d at index %d\n",target,i); 
    }
  }
  printf("target not found");
    return 0;
}
